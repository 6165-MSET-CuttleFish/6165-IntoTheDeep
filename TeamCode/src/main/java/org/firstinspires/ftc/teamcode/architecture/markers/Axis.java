package org.firstinspires.ftc.teamcode.architecture.markers;

/**
 * Used to specify spatial marker direction
    * it's very inconvenient to have a 2-axis spatial marker (cause when would the condition be satisfied?)
        * Hence, only one axis is allowed for a marker atm
 */
public enum Axis {
    X, Y, ANGLE
    
}
